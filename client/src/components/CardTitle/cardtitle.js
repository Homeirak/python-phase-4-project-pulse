// cardtitle.js
import React from "react";

function CardTitle({ title }) {
    return (
        <div className="card-title">
            <h3>{title}</h3>
        </div>
    );
}

export default CardTitle;
